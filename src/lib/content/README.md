# Editorial sources

The approved source material remains in `.old/codex-version/content/`. Public prose is preserved in the corresponding `src/routes/**/+page.svelte` files. These Svelte files are now the active implementation; do not run the archived Python builder to update them.

Clinic data lives in `clinics.ts`; route metadata lives in `pages.ts`. Check the approved source and root `NOTE-DE-APROBAT.md` before changing public copy. The old `.html` filenames in those editorial notes correspond to the extensionless SvelteKit routes. Legal placeholders and the article preparation state are deliberately retained.
